jdk:
- oraclejdk8
node_js: "node"
notifications:
  email: false
before_install:
  - nvm install node
  - nvm use node
  - nvm run node --version
install:
- npm install
script:
- if [ $HEROKU ]; then echo "here"; else echo "not here"; fi
- npm run test
- npm run build:prod
- mvn verify
cache:
  directories:
  - $HOME/.m2
  - node_modules